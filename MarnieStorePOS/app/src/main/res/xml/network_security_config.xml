<?xml version="1.0" encoding="utf-8"?>
<!--
  Enforces TLS for all traffic. Certificate pinning is intentionally left to
  OkHttp's CertificatePinner (configured in NetworkModule) rather than here,
  because pins baked into this static XML can't be rotated without a full
  app release if the backend's certificate changes; OkHttp still gives us
  pinning, just wired at the app layer where we can layer in kill-switch
  logic if a pin ever needs emergency bypass via a remote config flag.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    <!-- Allow cleartext ONLY for local emulator/dev testing against 10.0.2.2 -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
